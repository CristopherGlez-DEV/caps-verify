<!DOCTYPE html>
<html lang="es">
  <head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Gorra Original</title>
  <style>
    body {
      background-color: #000;
      color: #fff;
      font-family: 'Segoe UI', sans-serif;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      margin: 0;
      padding: 20px;
    }

    .card {
      background: #111;
      padding: 30px 20px;
      border-radius: 15px;
      box-shadow: 0 0 20px #222;
      text-align: center;
      width: 100%;
      max-width: 380px;
    }

    img {
      width: 100%;
      max-width: 220px;
      margin-bottom: 20px;
      filter: brightness(1.2);
      display: block;
      margin-left: auto;
      margin-right: auto;
    }

    h1 {
      color: #4ade80;
      font-size: 1.5rem;
      margin-bottom: 10px;
    }

    .info {
      font-size: 1rem;
      margin-top: 20px;
      color: #ccc;
    }

    .info strong {
      color: #fff;
    }
  </style>
</head>
  <body>
    <div class="card">
      <img src="<%= drop.logo %>" alt="<%= drop.name %> Logo" />
      <h1>✅ ¡Gorra original verificada!</h1>

      <div class="info">
        <p><strong>ID:</strong> <%= gorra.id %></p>
        <p><strong>Escaneos:</strong> <%= gorra.contador %></p>
      </div>
    </div>
  </body>
</html>
